export interface Translations {
  // Theme toggle
  theme: {
    light: string;
    dark: string;
    system: string;
    label: string;
  };
  // Language toggle
  language: {
    label: string;
    en: string;
    no: string;
  };
  // Section headings
  sections: {
    projects: string;
    experience: string;
    education: string;
    skills: string;
    about: string;
    hobbies: string;
    awards: string;
    volunteer: string;
  };
  // Common labels
  common: {
    present: string;
    readMore: string;
    viewProject: string;
    contact: string;
  };
}
